apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  type: ClusterIP
  selector:
    app: nginx
  ports:
    - name: http
      port: 8081
      protocol: TCP
      targetPort: 80
  externalIps:
      - 192.168.178.99

